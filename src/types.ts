export type Columns = { [key: string]: number };

export type Gap = { [key: string]: number };

export type Breakpoints = { [key: string]: number };

export type NormalizedBreakpoints = [string, number][];
